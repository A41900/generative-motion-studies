/* - físico:
     - applyFieldAsAcceleration
     - applyFieldAsImpulse
     - applySpringForce
     - integrate(dt)
//applyFieldAsAcceleration(p, field, accel);
//applyFieldAsImpulse(p, field, speed);
*/

// OTHER FILE ENERGY
// //damping
//decay visual
